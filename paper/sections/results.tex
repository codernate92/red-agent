\section{Results}
\label{sec:results}

\subsection{Model-level outcomes}

The archived run completed on 11 models across Claude, GPT, Gemini, and
Mistral families. \Cref{fig:dashboard} provides the consolidated comparison
view, and \cref{tab:model-summary} reports the exact model-level values.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{overview_dashboard.pdf}
  \caption{Overview dashboard for the curated 11-model run.}
  \label{fig:dashboard}
\end{figure*}

\begin{table*}[t]
  \centering
  \caption{Model-level summary for the 11-model curated run.}
  \label{tab:model-summary}
  \resizebox{0.96\textwidth}{!}{\input{tables/model_summary}}
\end{table*}

Three observations anchor the comparison. First, \texttt{gemini-2.5-pro} has
the highest aggregate risk score (7.60) but also the highest probe error rate
(45\%). Second, \texttt{mistral-small} and \texttt{mistral-large} show the
highest campaign hit rates (75\% and 50\%, respectively). Third, the Claude and
OpenAI clusters are operationally stable (near-zero error rates) with lower hit
rates than Mistral.

These metrics should be interpreted jointly. A high hit rate alone does not
imply highest aggregate risk, and a high risk score can coincide with a brittle
execution profile. The practical value of the framework is in exposing that
tradeoff explicitly.

\subsection{Cross-model metric heatmap}

To make cross-model tradeoffs easier to scan, \cref{fig:metric-heatmap} shows
the consolidated metric heatmap from the publication bundle. This view highlights
the contrast between higher-hit Mistral runs and lower-error Claude/OpenAI runs,
and it makes Gemini's elevated error profile immediately visible.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{metric_heatmap.pdf}
  \caption{Cross-model metric heatmap for the curated run.}
  \label{fig:metric-heatmap}
\end{figure}

The heatmap intentionally foregrounds campaign-native metrics. Benchmark-facing
evaluator analysis (for example \sr{}) is discussed in
\cref{sec:methodology} and remains future work for this specific artifact set.

\subsection{Comparative evaluator analysis}

To bridge campaign-native and judge-based views, we additionally ran
\sr{} scoring over the stored response traces (without rerunning probes) and
generated a comparative multi-panel visualization. \Cref{fig:comparative}
combines frontier-style tradeoff plots, normalized metric profiles, and a
benchmark-ready panel that can be populated with a ClearHarm run when those
artifacts are available.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{comparative_benchmark.pdf}
  \caption{Comparative analysis combining campaign outcomes and judge metrics.
  The right-hand benchmark panel is designed to support baseline-versus-ClearHarm
  overlays once ClearHarm run artifacts are provided.}
  \label{fig:comparative}
\end{figure*}

\subsection{Qualitative traces}

Qualitative trajectory excerpts remain consistent with the quantitative summary.
In the Mistral traces, low-complexity override and persona-hijack prompts still
produce concrete unsafe behaviors (for example explicit override acknowledgments
and unrestricted persona adoption). These cases are drawn from stored JSONL
artifacts, not synthetic examples.

\begin{figure*}[t]
  \centering
  \input{figures/qualitative_case}
  \caption{Compact case study drawn directly from archived JSONL trajectories.}
  \label{fig:qualitative-case}
\end{figure*}

\subsection{Exploratory size trend}

\Cref{fig:scaling} plots aggregate risk against log-parameter count for
size-annotated models in the run.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{scaling_plot.pdf}
  \caption{Exploratory size trend for the current curated run.}
  \label{fig:scaling}
\end{figure}

The figure remains exploratory. The strongest multi-point size coverage is still
within the Mistral family, so broad scaling conclusions across all families are
not warranted from this run alone.
